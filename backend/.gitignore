# --- PHP / Composer ---
vendor/
.phpunit.result.cache
composer.lock

# --- Variables de Entorno (SEGURIDAD) ---
.env
.env.local
.env.*.local
# Como el .env está en el frontend, subimos un nivel
../frontend/.env

# --- Firebase ---
# Ignora cualquier archivo JSON de credenciales
*.json
!composer.json
!package.json