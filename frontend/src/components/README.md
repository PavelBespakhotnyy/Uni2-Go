Reusable Svelte components used across the application.

Put here:

- UI components (Button.svelte, Card.svelte, Modal.svelte)
- Layout elements (Navbar, Sidebar)
- Small reusable components (Avatar, Badge, Spinner)

Rules:

- One component per file
- Use PascalCase for component names
