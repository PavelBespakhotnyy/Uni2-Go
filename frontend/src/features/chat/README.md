Chat feature module.

Put here:

- Chat UI components (ChatWindow, MessageList, MessageItem)
- Message composer component
- Chat stores (messages, typing status)
- Socket event handlers related to chat
